# ============================================================================
# GCP Deployment Configuration - PRODUCTION
# ============================================================================
# Copy this file to .env.production and fill in your values:
#   cp .env.production.example .env.production
#
# IMPORTANT: Never commit .env.production to version control!
# ============================================================================

# Required: GCP Project Configuration
DEPLOY_PROJECT_ID=my-gcp-project-prod
DEPLOY_BUCKET_NAME=my-app-production

# Required: Build Configuration
# Common values: dist (Vite, Vue), build (CRA, Svelte), out (Next.js), public (Hugo)
DEPLOY_BUILD_DIR=dist

# REQUIRED: Version Configuration
# You MUST specify a version for deployment. Choose one:
#
# Option 1: Manual version (recommended for production)
#   DEPLOY_VERSION=v1.0.0
#   DEPLOY_VERSION=v2.1.3
#
# Option 2: Auto-detect from git tags (best for CI/CD)
#   DEPLOY_VERSION=auto
#   Requires: git tag v1.0.0
#   Results in: v1.0.0-{git-sha}
#
# Override with CLI: ./deploy-production.sh --version=v2.0.0
DEPLOY_VERSION=

# Optional: Release Path Configuration
# Specify where to deploy files within the bucket
#
# Default: Bucket root (empty) - Recommended for simplicity
#   DEPLOY_RELEASE_PATH=
#   This deploys directly to gs://bucket/ (default behavior)
#
# Custom folder paths:
#   DEPLOY_RELEASE_PATH=releases/
#   DEPLOY_RELEASE_PATH=app/
#   DEPLOY_RELEASE_PATH=frontend/v1/
#   Note: Paths will automatically have / added at the end if missing
#
# Explicit bucket root (same as leaving empty):
#   DEPLOY_RELEASE_PATH=.
#   DEPLOY_RELEASE_PATH=/
#   DEPLOY_RELEASE_PATH=root
#
# Override with CLI: ./deploy-production.sh --release-path=releases/
#
# WARNING: Deploying to bucket root uses 'gsutil rsync -d' which DELETES
# files in the bucket that are not in your build directory! Use custom
# paths if you want to keep multiple versions or other files in the bucket.
DEPLOY_RELEASE_PATH=

# Optional: GCP Regional Settings
DEPLOY_REGION=us-central1
DEPLOY_BUCKET_LOCATION=US

# Optional: Cache Configuration (Production - longer cache times)
# Static assets (JS, CSS, images): 1 year with immutable flag
DEPLOY_CACHE_MAX_AGE=31536000
# HTML files: 1 hour (allows faster updates while still caching)
DEPLOY_HTML_CACHE_MAX_AGE=3600

# Optional: Compression Settings
DEPLOY_GZIP_EXTENSIONS=js,css,html,json,svg,txt,xml

# Optional: Load Balancer Configuration
# Set these if you want the script to show LB update instructions after deployment
DEPLOY_URL_MAP_NAME=
DEPLOY_PATH_MATCHER_NAME=path-matcher-1

# Deprecated: Leave empty (manual LB updates recommended)
DEPLOY_BACKEND_BUCKET_NAME=
DEPLOY_ENABLE_LB_UPDATE=false
