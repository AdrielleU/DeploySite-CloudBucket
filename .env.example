# ============================================================================
# GCP Deployment Configuration - EXAMPLE
# ============================================================================
# This is a generic example. For environment-specific configs, use:
#   .env.staging.example  - For staging environment
#   .env.production.example - For production environment
#
# Copy this file to create your environment config:
#   cp .env.example .env.staging
#   cp .env.example .env.production
# ============================================================================

# Required: GCP Project Configuration
DEPLOY_PROJECT_ID=your-gcp-project-id
DEPLOY_BUCKET_NAME=your-bucket-name

# Required: Build Configuration
# Specify the directory where your build outputs files
# Common values by framework:
#   - React (Vite): dist
#   - React (CRA): build
#   - Next.js (static): out
#   - Angular: dist/project-name
#   - Vue: dist
#   - Svelte/SvelteKit: build
#   - Hugo: public
#   - Jekyll: _site
DEPLOY_BUILD_DIR=dist

# REQUIRED: Version Configuration
# You MUST specify a version for deployment. Choose one of these options:
#
# Option 1: Manual version (recommended for most cases)
#   DEPLOY_VERSION=v1.0.0
#   DEPLOY_VERSION=v2.1.3
#   DEPLOY_VERSION=v1-0-0    # Use dashes if you prefer
#
# Option 2: Auto-detect from git tags (requires git tags to be created first)
#   DEPLOY_VERSION=auto
#   This will use the latest git tag + commit SHA (e.g., v1.0.0-abc123)
#
# How to use git tags:
#   git tag v1.0.0              # Create a tag at current commit
#   git tag v1.0.0 abc123       # Tag a specific commit
#   git tag -a v1.0.0 -m "Release v1.0.0"  # Annotated tag with message
#   git push origin v1.0.0      # Push tag to remote (optional)
#   git tag -l                  # List all tags
#
# You can also override this with --version flag:
#   ./deploy-staging.sh --version=v2.0.0
#
DEPLOY_VERSION=

# Optional: GCP Regional Settings
DEPLOY_REGION=us-central1
DEPLOY_BUCKET_LOCATION=US

# Optional: Cache Configuration
# For staging, use shorter times:
#   DEPLOY_CACHE_MAX_AGE=86400        # 24 hours
#   DEPLOY_HTML_CACHE_MAX_AGE=1800    # 30 minutes
# For production, use longer times:
#   DEPLOY_CACHE_MAX_AGE=31536000     # 1 year
#   DEPLOY_HTML_CACHE_MAX_AGE=3600    # 1 hour
DEPLOY_CACHE_MAX_AGE=86400
DEPLOY_HTML_CACHE_MAX_AGE=1800

# Optional: Compression Settings
# Comma-separated list of file extensions to gzip compress
DEPLOY_GZIP_EXTENSIONS=js,css,html,json,svg,txt,xml

# Optional: Load Balancer Configuration
# If you have a load balancer with path rewriting, set these values
# to get automatic instructions after deployment
DEPLOY_URL_MAP_NAME=
DEPLOY_PATH_MATCHER_NAME=path-matcher-1

# Deprecated: Legacy options (leave empty)
DEPLOY_BACKEND_BUCKET_NAME=
DEPLOY_ENABLE_LB_UPDATE=false
